name: tranzfort_tms
description: "Tranzfort Transport Management System"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_driver:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^1.2.1
  intl: ^0.20.2
  path_provider: ^2.1.4
  provider: ^6.1.2
  flutter_riverpod: ^2.4.9
  drift: ^2.14.0
  sqlite3: ^2.4.0
  sqlite3_flutter_libs: ^0.5.24
  path: ^1.8.0
  archive: ^3.3.0
  crypto: ^3.0.3
  share_plus: ^7.0.2
  image_picker: ^1.0.4
  pdf: ^3.10.4
  permission_handler: ^11.1.0
  geolocator: ^14.0.2
  uuid: ^4.5.2
  latlong2: ^0.9.1
  excel: ^4.0.2
  flutter_map: ^6.1.0
  flutter_map_tile_caching: ^9.0.0
  cached_network_image: ^3.3.1
  easy_localization: ^3.0.0
  fl_chart: ^0.66.0
  flutter_local_notifications: ^17.0.0
  timezone: ^0.9.0
  shared_preferences: ^2.2.0
  speech_to_text: ^7.0.0
  flutter_tts: ^4.0.0
  tflite_flutter: ^0.10.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.7
  drift_dev: ^2.14.0
  mockito: ^5.4.4

flutter:
  uses-material-design: true
  assets:
    - assets/translations/
  # fonts:
  #   - family: Roboto
  #     fonts:
  #       - asset: assets/fonts/Roboto-Regular.ttf
  #       - asset: assets/fonts/Roboto-Bold.ttf
  #       - asset: assets/fonts/Roboto-Italic.ttf

# For Android
# minSdkVersion: 21
# targetSdkVersion: 34

# For iOS
ios:
  platform: ios
  deployment_target: '13.0'
  minimum_ios_version: '13.0'

# For Windows
windows:
  platform: windows
  version: '10.0.0'

# For Linux
linux:
  platform: linux

# For app signing (production)
# flutter build --release
# flutter pub run --release
# flutter build apk --release
# flutter build appbundle --release
# flutter build web --release
